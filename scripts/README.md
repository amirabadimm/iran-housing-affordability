# Scripts

`copy_local_data.py` copies the local working data files from `F:/RA/Presentation/Cleaned` into `data/cleaned/`.

This is only a convenience script for local reproduction. The large HBS CSV files remain ignored by git.
